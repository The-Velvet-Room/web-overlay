// specifying files explicitly in the tsconfig causes
// problems when adding new files, so we'll alias the
// type definitions here to avoid having to reference
// them in the tsconfig

/// <reference path="../typings/browser.d.ts" />